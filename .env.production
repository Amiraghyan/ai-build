# .env.production
# URL où le front doit joindre le micro-service FastAPI dans le réseau Docker Compose.
# L’hôte « backend » correspond au nom du service déclaré dans docker-compose.yml.
VITE_API_BASE_URL=http://backend:8000
